{
  "name": "countries-historical",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "yarn build && serve ./dist"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "serve": "^14.2.4",
    "vite-plugin-pwa": "^1.0.0"
  }
}
